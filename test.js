'use strict';

var chai = require('chai'),
    should = chai.should(),
    assert = chai.assert;

describe('Test', function() {
    it('works', function(done) {
        assert.equal(true, true);
        done();
    });
});